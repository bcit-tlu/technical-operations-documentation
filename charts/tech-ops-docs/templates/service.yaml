apiVersion: v1
kind: Service
metadata:
  name: {{ include "tech-ops-docs.fullname" . }}
  labels:
    {{- include "tech-ops-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tech-ops-docs.selectorLabels" . | nindent 4 }}
